package com.shopflow.repository;
import com.shopflow.entity.Brand;
import org.springframework.data.jpa.repository.JpaRepository;
import 
public interface BrandRepository extends JpaRepository<Brand,Long> {  }
