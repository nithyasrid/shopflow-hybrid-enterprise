Optional BigQuery target. Keep service-account JSON files out of Git.

The local project works without GCP. Create the warehouse from `warehouse/bigquery_schema.sql` when you want to demonstrate BigQuery.
